{
  "ruleId": "AP-7.2.1-CWD-CONTAINMENT",
  "confidence": "core",
  "spec": "7.2.1",
  "title": "a ${PLUGIN_ROOT}-rooted cwd that escapes after resolution is invalid",
  "rationale": "The form is permitted, so only post-resolution containment catches this. It is the case a prefix check misses.",
  "quote": "A plugin-relative or `${PLUGIN_ROOT}`-rooted value MUST remain within the filesystem-resolved plugin root.",
  "observability": "report",
  "expect": {
    "rejected": null,
    "loaded": {
      "skills": [],
      "mcpServers": [
        "fine"
      ]
    },
    "skipped": [
      {
        "what": "mcp.json#escaping",
        "ruleId": "AP-7.2.1-CWD-CONTAINMENT"
      }
    ],
    "reported": []
  }
}
