{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "escaping": {
      "type": "stdio",
      "command": "node",
      "cwd": "${PLUGIN_ROOT}/../elsewhere"
    },
    "fine": {
      "type": "stdio",
      "command": "node",
      "cwd": "${PLUGIN_ROOT}/work"
    }
  }
}
