{
  "ruleId": "AP-7.2.1-VARIANT-CLOSED",
  "confidence": "core",
  "spec": "7.2.1",
  "title": "a streamable-http entry carrying command and args is invalid",
  "rationale": "The mirror of the stdio-with-url case, and the shape a copy-pasted config produces.",
  "quote": "Each server configuration MUST contain a `type` field and match exactly one of the closed variants below. An unknown field, an unknown `type` value, or a field belonging to another variant makes that server entry invalid.",
  "observability": "report",
  "expect": {
    "rejected": null,
    "loaded": {
      "skills": [],
      "mcpServers": [
        "fine"
      ]
    },
    "skipped": [
      {
        "what": "mcp.json#mixed",
        "ruleId": "AP-7.2.1-VARIANT-CLOSED"
      }
    ],
    "reported": []
  }
}
