{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "expanded": {
      "type": "stdio",
      "command": "${PLUGIN_ROOT}/bin/server"
    },
    "fine": {
      "type": "stdio",
      "command": "./bin/server"
    }
  }
}
