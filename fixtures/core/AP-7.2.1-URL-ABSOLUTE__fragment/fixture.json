{
  "ruleId": "AP-7.2.1-URL-ABSOLUTE",
  "confidence": "core",
  "spec": "7.2.1",
  "title": "a url carrying a fragment is invalid",
  "rationale": "A fragment is never sent to the server, so it can only mislead.",
  "quote": "The `url` value MUST be an absolute HTTP or HTTPS URL and MUST NOT contain user information or a fragment.",
  "observability": "report",
  "expect": {
    "rejected": null,
    "loaded": {
      "skills": [],
      "mcpServers": [
        "fine"
      ]
    },
    "skipped": [
      {
        "what": "mcp.json#frag",
        "ruleId": "AP-7.2.1-URL-ABSOLUTE"
      }
    ],
    "reported": []
  }
}
