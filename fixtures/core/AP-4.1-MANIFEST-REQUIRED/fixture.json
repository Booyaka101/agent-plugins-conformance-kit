{
  "ruleId": "AP-4.1-MANIFEST-REQUIRED",
  "confidence": "core",
  "spec": "4.1",
  "title": "a directory with no plugin.json is not a plugin",
  "rationale": "The manifest is the conformance floor. Without it there is nothing to load, however many skills the directory contains.",
  "quote": "A plugin MUST include a manifest at `plugin.json` in the plugin root.",
  "observability": "report",
  "expect": {
    "rejected": "manifest-missing",
    "loaded": {
      "skills": [],
      "mcpServers": []
    },
    "skipped": [],
    "reported": []
  }
}
