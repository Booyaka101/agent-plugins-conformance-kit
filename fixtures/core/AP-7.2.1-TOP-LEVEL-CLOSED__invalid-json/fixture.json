{
  "ruleId": "AP-7.2.1-TOP-LEVEL-CLOSED",
  "confidence": "core",
  "spec": "7.2.1",
  "title": "mcp.json that is not valid JSON disables MCP and nothing else",
  "rationale": "A parse error in one component file must not sink the plugin.",
  "quote": "`mcp.json` MUST be a JSON object containing the required `$schema` and `mcpServers` fields, with no other top-level fields.",
  "observability": "report",
  "expect": {
    "rejected": null,
    "loaded": {
      "skills": [
        "alpha"
      ],
      "mcpServers": []
    },
    "skipped": [
      {
        "what": "mcp.json",
        "ruleId": "AP-7.2.1-TOP-LEVEL-CLOSED"
      }
    ],
    "reported": []
  }
}
