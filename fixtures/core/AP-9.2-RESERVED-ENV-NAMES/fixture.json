{
  "ruleId": "AP-9.2-RESERVED-ENV-NAMES",
  "confidence": "core",
  "spec": "9.2",
  "title": "an env entry named PLUGIN_ROOT invalidates that server entry",
  "rationale": "The client supplies both reserved variables. A plugin that sets one is trying to lie to its own subprocess about where it lives.",
  "quote": "An MCP server's `env` object MUST NOT contain entries named `PLUGIN_ROOT` or `PLUGIN_DATA`. Such an entry makes that server configuration invalid under §7.2.2.",
  "observability": "report",
  "expect": {
    "rejected": null,
    "loaded": {
      "skills": [],
      "mcpServers": [
        "fine"
      ]
    },
    "skipped": [
      {
        "what": "mcp.json#reserved",
        "ruleId": "AP-9.2-RESERVED-ENV-NAMES"
      }
    ],
    "reported": []
  }
}
